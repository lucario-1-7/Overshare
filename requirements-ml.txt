# Phase 2+ — heavy ML stack (NOT needed for Phase 1).
#
# NOTE: as of this build the local interpreter is Python 3.14, which may not yet
# have wheels for torch / paddlepaddle. If installs fail, create a dedicated
# Python 3.11 or 3.12 venv for the ML stack:
#     py -3.11 -m venv .venv-ml
#
# Install the correct CUDA build of torch FIRST, e.g. for CUDA 12.1:
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

ultralytics>=8.1          # YOLOv8 (stock COCO) — objects, home indicators, documents
retina-face>=0.0.17       # RetinaFace — face DETECTION only (never recognition)
paddleocr>=2.7            # OCR (primary)
paddlepaddle-gpu          # PaddleOCR GPU backend
easyocr>=1.7              # OCR fallback
presidio-analyzer>=2.2    # PII detection over caption + OCR text
presidio-anonymizer>=2.2
spacy>=3.7
requests>=2.31            # footprint existence checks
# spaCy model (run after install):  python -m spacy download en_core_web_lg
